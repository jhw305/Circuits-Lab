#!/bin/bash

# TODO This needs to be autogenerated next and then put in the Makefile. Take out data directory as well and just have tables.
./main.py
for fname in ./tables/*.csv ; do
	../../common/scripts/gen_plt.py $fname "./images/$(basename $fname .csv).PNG"
done
